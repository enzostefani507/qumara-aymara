language: python
python:
    - 3.8
services:
    - postgresql
install:
    - pip install -r requirements.txt
deploy:
    provider: heroku
    api_key: $HEROKU_API_KEY
    app: qumara-aymara
    run: python manage.py migrate
    on: main